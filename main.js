//server.js dans openclassroom

//const http = require('http'); // Fait appel à http.js
const app = require('./server'); // 

//const server = http.createServer(app); // Créer le serveur
//app.set('port', process.env.PORT || 3000); // définir le port du serveur
//server.listen(process.env.PORT || 3000); // demander au serveur d'écouter sur le port 3000